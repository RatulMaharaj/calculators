---
// add props
const { title, description, favicon } = Astro.props;
import "@fontsource-variable/montserrat";
import "@fontsource/schoolbell";
import "../styles/global.css";
---

<html lang="en" data-theme="garden">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" href={`https://fav.farm/${favicon ?? "🧮"}`} />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="generator" content={Astro.generator} />
    <title>{title ?? `calcs.co.za`}</title>
    <meta
      name="description"
      content={description ?? "Calculators made for South Africans 🇿🇦"}
    />
  </head>
  <body class="p-4 flex flex-col items-center">
    <header>
      <a href="/" class="font-serif text-lg font-semibold hover:underline"
        >calcs.co.za</a
      >
    </header>
    <main class="w-full sm:max-w-md">
      <div class="flex flex-col mt-2 mb-8 gap-y-1">
        {title && <h1 class="text-center text-2xl font-bold">{title}</h1>}
        {
          description && (
            <h2 class="text-center -mt-2 text-sm">{description}</h2>
          )
        }
      </div>
      <slot />
    </main>
  </body>
</html>
